# SimpleAuth 設定ファイル

# プレイヤーが入力するパスワード。コマンドは /auth <パスワード> になります。
password: "password"

# 参加してから認証するまでの猶予時間（秒）。
# この時間内に /auth <パスワード> を入力しないとキックされます。
timeout-seconds: 30

# true にすると、一度認証したプレイヤーは次回以降パスワード入力なしで自動ログインします。
# false にすると毎回パスワードが必要になります。
remember-players: true
